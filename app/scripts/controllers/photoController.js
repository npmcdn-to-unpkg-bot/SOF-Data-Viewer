'use strict';

/**
 * @ngdoc function
 * @name sofDataViewerApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the sofDataViewerApp
 */
angular.module('sofDataViewerApp')
  .controller('photoController', function ($scope) {
    // var ref = new Firebase("https://sof-data.firebaseio.com");
    // $scope.alStudents = $firebaseObject(ref);
    // $scope.allStudents = $firebaseArray(ref);
  });
